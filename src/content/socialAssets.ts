import { publicImages } from '@/content/assetPaths'

/** Shared social icon assets + outbound URLs (header + join section). */
export const TELEGRAM_ICON_SRC = publicImages.iconTelegram
export const X_ICON_SRC = publicImages.iconX
export const TELEGRAM_URL = 'https://t.me/COMMONWEALTHTOKENOFFICIAL' as const
export const X_URL = 'https://x.com/CWUblockchain' as const
